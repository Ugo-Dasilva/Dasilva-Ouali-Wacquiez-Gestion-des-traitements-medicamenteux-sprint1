{% extends 'base.html.twig' %}

{% block title %}Affichage des patients {% endblock %}

{% block body %}

                L'id du patient est :{{lespatients.getId}}
                <br>
                Le nom du patient est :{{lespatients.getnom}}
                <br>
                Le prenom  du patient est :{{lespatients.getprenom}}
                <br><br>
                {% for lessejours in lespatients.getSejour %}
                La date de debut du  sejour du patient est : {{lessejours.getdatedebut|date('Y-m-d')}}
                <br><br>
                                {% for lestraitements in lessejours.getTraitement %}
                                    Les traitements sont (dans le sens inverse chronologique) :<br>
                                    La date de debut du traitement est : {{lestraitements.getDatedebut|date('Y-m-d')}}
                                    <br>
                                    La date de fin du traitement est : {{lestraitements.getDateFin|date('Y-m-d')}}
                                    <br><br>
                                    {% for lesindications in lestraitements.getindication %}
                                        Les medicaments sont:  {{lesindications.getmedicament.getlibelle}}
                                        <br>
                                    {% endfor %}
                                {% endfor %}
                {% endfor %}
                
               

                
                

   
{% endblock %}